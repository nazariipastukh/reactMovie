import {MoviesList} from "../../components";
import {PaginationComponent} from "../../components";

export const MoviesPage = () => {

    return (
        <div>
            <MoviesList/>
            <PaginationComponent/>
        </div>
    );
};